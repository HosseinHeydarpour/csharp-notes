<Window x:Class="TodoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TodoApp"
        mc:Ignorable="d"
        Title="Todo Application V1" Height="450" Width="400" ResizeMode="NoResize">
    <!-- ResizeMode="NoResize" to prevent window resizing -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>


        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
            <RowDefinition Height="10" />
            <RowDefinition Height="75" />
            <RowDefinition Height="10" />
            <RowDefinition Height="50" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>

        <Button Grid.Row="5" Grid.Column="1" Content="Create Todo" Click="AddToDoButton_Click" />

        <TextBox x:Name="TodoInput" Grid.Row="3" Grid.Column="1" Background="#FF1B1740" Foreground="White" />

        <!-- VerticalScrollBarVisibility="Auto" it will automatically add scrollbar if it is scrollable -->
        <ScrollViewer Grid.Column="1" Grid.Row="1" VerticalScrollBarVisibility="Auto" Background="#FF1F204A" >
            <StackPanel Background="#FF1F204A" x:Name="TodoList"></StackPanel>
        </ScrollViewer>



    </Grid>
</Window>
